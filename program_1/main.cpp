// C++ Implementation

#include <stdio.h>
#include <stdlib.h>
#include <pthread.h>
#include <iostream>
using namespace std;

void finder ()
{
   ofstream datafile;
   datafile.open ("Human_MtDNA.txt", ios::out | ios::app | ios::binary);
   if (myfile.is_open()) {
      cout << "Text File is Open" << endl;
   }
}

int main ()
{
   finder ();
}